#include <iostream>

using namespace std;

// 아스키 코드
int main()
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);

    char S;

    cin >> S;

    cout << int(S);

    return 0; // 정상종료
}