#include <iostream>

using namespace std;

// 수학은 체육과목 입니다
int main()
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);

    long n;
    cin >> n;

    cout << 4 * n;

    return 0; // 정상종료
}