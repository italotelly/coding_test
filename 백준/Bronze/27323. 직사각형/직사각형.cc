#include <iostream>

using namespace std;

// 직사각형
int main()
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);

    int A, B;

    cin >> A >> B;

    cout << A * B;

    return 0; // 정상종료
}